// First child in javascript

console.log(document.body.firstChild)  // #text (Because after body there is space)

// Returns the first child and that could be anything like element, text, comment etc

// Last child in  javascript 

console.log(document.body.lastChild) // script file will show which is true

// child nodes

console.log(document.body.childNodes) // printed 54 Nodelist of the html file

//Chilnodes are not array they are node list 

// Defination: ChildNodes returns all the children in our case we target body so all the body children will return

console.log(document.body.hasChildNodes) //ƒ hasChildNodes() { [native code] }

// Checks whether the presence of childNodes are not


